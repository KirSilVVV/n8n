# Archived SQL migrations

These were the first-pass migrations using `ai_*` table prefix in `public` schema.
Replaced by canonical versions in `sql/` that use `ai.*` schema (created by Саша as isolated namespace).

Archived 7 May 2026.
